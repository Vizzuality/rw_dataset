#! /bin/bash

ps -ef | grep 3000 | grep puma
if [ $? -eq 1 ]
then
  echo "Server is down."
  bundle exec puma -t 5:5 -d -p 3000 -e production -S ~/puma -C config/puma.rb
else
  echo "Server running ;)!"
fi
